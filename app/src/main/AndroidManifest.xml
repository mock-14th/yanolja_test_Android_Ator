<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rising_test_yanolja">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".config.ApplicationClass"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RisingTestYanolja"
        >

        <activity android:name=".src.login.LoginActivity"
            android:theme="@style/LoginActivity"/>

        <activity android:name=".src.signUp.SignUpActivity2"
            android:theme="@style/LoginActivity"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".src.signUp.SignUpActivity1"
            android:theme="@style/LoginActivity"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <activity android:name=".src.main.MainActivity">
        </activity>
        <activity android:name=".src.setting.SettingActivity"/>
        <activity android:name=".src.productInfo.ProductInfoActivity"
            android:theme="@style/SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".src.splash.SplashActivity"
            android:theme="@style/SplashActivity">

        </activity>
    </application>

</manifest>